# WU Plan — расписание без боли
Мобильное приложение перестало работать 21.04.2026

Официальный сайт wu.cdv.pl работает как будто его писали в 2008-м. Тормозит, не адаптирован под телефон, и вообще — не PWA. Поэтому я написал своё.

Это веб-приложение для студентов Collegium Da Vinci (Wirtualna Uczelnia). Работает как нативное приложение на iPhone и Android, кешируется офлайн, приятный UI. 

---

## Что умеет

- Логин через твои существующие WU-credentials
- Расписание на неделю вперёд и назад
- Карточки занятий с деталями: преподаватель, аудитория, ссылка на онлайн
- Офлайн-режим — данные берутся из кеша, показывается когда они были получены
- Автовход при повторном открытии
- Онбординг при первом запуске
- Dark mode (OLED black), Light mode, три языка: PL / RU / EN
- Устанавливается на домашний экран — работает как приложение

---

## Приватность

Сервер — чистый прокси. Он принимает твой логин и пароль, пробрасывает запрос на wu.cdv.pl, и отдаёт обратно сессионный токен. Всё.

- **Пароли нигде не логируются** — ни в файлы, ни в консоль, ни в базу данных
- **На сервере ничего не хранится** — никакой базы, никаких сессий, никакого стейта
- **Credentials лежат только у тебя** — в `localStorage` твоего устройства в base64
- **Кеш расписания** — тоже только в `localStorage`, не покидает устройство
- Хочешь убедиться — исходники открыты, смотри `server.js`

---

## Запустить локально

```bash
git clone https://github.com/ikratenko-rgb/CollegiumDaVinci-wu-mobile-app.git
cd CollegiumDaVinci-wu-mobile-app
npm install
npm start
```

Открывай `http://localhost:3000`. Для тестирования на телефоне в одной сети — узнай свой локальный IP и открывай `http://192.168.x.x:3000`.

---

## Стек

- **Frontend**: Native JavaScript (ES6+), Zero-Dependency Architecture.
- **Backend**: Node.js + Express (прокси к wu.cdv.pl)
- **PWA**: Service Worker, Web App Manifest, localStorage cache

---

## Контрибьютить

Нашёл баг или хочешь добавить фичу — велком.

```bash
git fork
git checkout -b fix/my-fix
# делай что надо
git commit -m "fix: описание что починил"
git push origin fix/my-fix
# открывай PR
```

Пиши мне в Telegram если что-то сломалось или есть идеи: [@krtlnk](https://t.me/krtlnk)

---

*Сделано студентом для студентов. Без корпоративного вайба, но с нормальным UX.*
